This script needs the
plugin spigot plugin
"Skript"!

made by twitter.com/kraagebein

Commands:
/autobc (help) or (manual) or (on) or (off)

Permissions:
autobc.use (Main and only permission)